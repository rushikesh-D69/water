# Python
__pycache__/
*.py[cod]
*.pyo
.Python
*.egg-info/

# Jupyter checkpoints
.ipynb_checkpoints/

# Large raw data (re-downloadable from NASA)
data/exoplanets_raw.csv

# Keep processed data and ranking (results)
!data/exoplanets_processed.csv
!data/final_priority_ranking.csv
!data/static_priority_ranking.csv

# Models (optional - uncomment to exclude if too large)
# models/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Report compiled outputs (excluded per project convention)
report/*.aux
report/*.bbl
report/*.blg
report/*.log
report/*.out
report/*.pdf
report/*.toc

# Stage 3 RL — SB3 Monitor logs and TensorBoard
logs/
tb_logs/
*.monitor.csv

# Stage 3 RL — Intermediate PPO checkpoints (keep final only)
models/ppo_phase1.zip
models/ppo_phase2.zip
